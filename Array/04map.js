// it is most used in react
// so, it will take an arrow function and according to that function it will modify the array.

const num = [1, 2, 3, 4, 5, 6];

const ans = num.map( x => x * 2);

console.log(ans);